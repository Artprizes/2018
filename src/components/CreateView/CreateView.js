import React, { Component } from 'react';
import { Text, View } from 'react-native';
import ArtDescription from '../artdescription';

const CreateView = () => <ArtDescription />;

export default CreateView;
